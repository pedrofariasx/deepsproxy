services:
  deepsproxy:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      PORT: ${PORT:-3000}
      API_KEY: ${API_KEY:-}
    volumes:
      - ./deepseek_profile:/app/deepseek_profile
    restart: unless-stopped
